// next-sitemap.js
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://tactics-digital-agency.vercel.app/",
  generateRobotsTxt: true, // (optional)
};

export default config;
